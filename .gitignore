# dependencies
node_modules/
.pnpm-store/

# builds
dist/
build/
.next/
out/
*.tsbuildinfo

# env
.env
.env.*
!.env.example

# test / coverage
coverage/
playwright-report/
test-results/
.turbo/

# local-only (never published)
docs/

# Media the pipeline produces: uploaded sources, transcoded renditions, HLS
# segments. Every byte of it is regenerated by `pnpm db:seed`, which synthesises
# its own source video with ffmpeg, so none of it belongs in git.
var/

# logs
*.log
npm-debug.log*

# os
.DS_Store

# Prisma client is generated into the package (see schema.prisma) and rebuilt
# by `pnpm db:generate`; it is a build output, not source.
packages/db/generated/

# Demo GIF frames are regenerated by scripts/demo-gif.sh; only the GIF is committed.
e2e/demo-shots/
